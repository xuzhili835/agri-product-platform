# 农产品融销一体平台 - 项目设计文档

> 版本：v1.5
> 更新时间：2026-07-15（合并原"第七节 实现备注"至各节，文档与实现对齐）
> v1.4 更新：2026-07-16（专家模块整改——新增追问回复表 tb_question_reply、问答/预约真实姓名冗余、预约期望时间、问答「关闭/结束对话」状态、删除权限校验）
> v1.5 更新：2026-08-14（新增 AI 智能助手模块——手写 ReAct 编排循环、8 工具 function calling、写操作确认门、PII 脱敏、RAG 知识检索、审计日志、全局熔断；新增 5 张表 tb_agent_session/tb_agent_message/tb_agent_tool_log/tb_system_config/tb_knowledge_chunk）
> v1.6 更新：2026-08-15（AI 助手安全与正确性整改：confirm/history/会话三处属主校验防越权与 IDOR；confirm 响应结构化 status 防误报"已执行"；同会话单 pending 防顺序双写下单；apply_finance/reserve_expert 预览补槽位校验与专家存在性校验、draft 电话脱敏；RAG 索引改增量对账（新文章自动入索引）；编排 55s 耗时预算防前端超时后重复请求；toggle 改 upsert。二轮"假执行/表单"复查：apply_finance 移除 repayment 假参数（落库固定用套餐期限）；金额加套餐额度上限校验；联合贷款人存在性/角色校验前移到预览；confirm 的 sessionId 补属主校验（防污染他人历史）；聊天历史按 (create_time, id) 双键排序（同秒消息顺序稳定）；provider 解析异常纳入 fallback（怪响应不再 500）；chat 入口校验空/超长消息（防空 content 打爆 API 400）；下单数量上限 999；chat 单次超时降 25s 保证主+fallback 链不破总预算）
> v1.7 更新：2026-08-15（三轮实测整改——修复"模型纯文字伪造确认卡/执行结果"假执行事故（实测复现：编造"海南新鲜芒果¥35/订单号14"，工具未调用、库中无此订单）：编排层假话术检测（待确认操作/即将下单…确认执行?/下单成功,订单号/为您下单等）+纠偏指令自动重试一轮；确认卡与执行结果回灌历史时摘要化/加系统标注（断绝模型模仿样本）；System Prompt 第9/10条禁止模仿系统格式；商品编号会话白名单 SearchedProductCache——place_order 只接受本会话搜索验证过的编号，未命中时按编号反查标题自动代搜（命中即放行、用户在确认卡二次核对，不存在则回灌在售列表），杜绝模型编造编号/把"订单号"当"商品编号"；写工具描述改为"信息齐全直接调用、调用后自动出卡"。浏览器端到端实测：苹果/芒果下单全链路真卡+落库验证通过，23/23 单测绿）
> v2.1 更新：2026-08-16（七轮实测整改——"用新地址下单"仍落默认地址：模型缺少引用新地址的便捷方式（需翻 list_addresses 拿编号，实测偷懒传'默认地址'）。新增 LatestAddressCache 会话缓存：add_address 落库后反查新地址编号并记录、返回文本带"地址簿#N"；place_order 的 address 支持'新地址/最新地址/刚新增'快捷引用；System Prompt：用户说"用新地址/继续下单"时默认新地址。PII 询问拦截扩展（"收件人需要特别指定吗"类变体）；add_address 详细地址自动去重区名前缀。API 端到端验证：改地址→新增（自动取注册资料）→用新地址下单卡上地址正确为长沙→订单落库）
> v2.0 更新：2026-08-16（六轮实测整改：①新增地址的收件人/手机号改为可选——自动回填账号注册资料（与预约同口径，PII 不经 LLM），null/空串占位均触发回填，资料缺失才要求补充；System Prompt 第 11 条禁止向用户索要账号已有资料，编排层新增"请提供…手机号/收件人"话术检测并入纠偏重试管道；②写工具 execute 返回文案必须用户向——"可对买家说'用新地址下单'"类内部指令曾直接展示给用户，全工具审查仅 add_address 一处已改。实测：一句话新地址下单直接出卡，收件人赵晓东/电话自动取注册资料并脱敏显示）
> v1.9 更新：2026-08-16（五轮系统性横向排查——按"角色错配/漏必填槽/表单规则不一致"三模式审计全部工具：①写表 NOT NULL 无默认列 vs 工具传参逐一比对，槽位无遗漏；②apply_finance 首次端到端落库验证通过（期限按套餐/利率/银行冗余/还款来源15字预览追问），至此 4 个写工具全部经浏览器端到端落库验证；③query_credit 限 farmer 与前端展示一致（buyer 无信用分入口）、preferred_time 文本与纯文本渲染兼容——两项核实无误不改；④拦截误伤修正：将来时预告（"我将…提交"）不再拦，保留完成式伪造/确认卡格式/现在时承诺/系统标注泄露拦截；兜底文案通用化）
> v1.8 更新：2026-08-16（四轮实测整改：①买家开放预约专家（业务层不限角色，工具误配 farmer 改 common）；②下单地址簿化——新增 list_addresses/add_address 工具，place_order 只接受"默认地址"或"地址簿#编号"，自由文本地址一律拒绝，新地址经 add_address 确认门落地址簿（省/市/区/详细/收件人/电话三级区域结构）；③"改成X"换购语义——新增 list_my_orders/cancel_order，换购时先提醒旧待付款单、确认取消后再买，不再默默叠单；④预约补 area（种植面积）必填槽（tb_reserve.area NOT NULL，漏传在确认后才 SQL 失败）；⑤拦截加强：空承诺话术匹配放宽至 60 字距、系统标注([系统确认卡:…])泄露检测。端到端实测：buyer 预约落库/新地址落簿并正确引用上卡/换购提醒+取消+新单落库全部通过）

---

## 一、用户角色

| 角色 | 代码 | 权限 |
|:---|:---|:---|
| 农户 | `farmer` | 发布农产品、融资申请、专家咨询、个人中心 |
| 买家 | `buyer` | 浏览商品、下单购买、购物车、专家咨询、个人中心 |
| 专家 | `expert` | 发布知识、回答问题、查看预约 |
| 银行 | `bank` | 审批融资、智能匹配 |
| 管理员 | `admin` | 全部管理功能 |

> **角色获取方式**：农户/买家由自助注册（`POST /user/register`）获得；专家/银行由管理员添加或经"角色申请"审核开通；管理员仅由管理员添加（角色申请不允许申请 admin）。前端注册页仅提供「农户/买家」两个角色，专家/银行需先注册登录后在「个人中心 → 角色申请」提交申请。
> **个人中心入口**：farmer/buyer/expert 进入各自个人中心页；admin/bank 无个人中心页，点击头像回首页；默认头像统一为 `/images/default-avatar.png`。

---

## 二、数据库设计

### 表清单（23张表）

| 序号 | 表名 | 说明 |
|:---:|:---|:---|
| 1 | tb_user | 用户表 |
| 2 | tb_address | 收货地址表 |
| 3 | tb_product | 商品表（货源/需求） |
| 4 | tb_shoppingcart | 购物车表 |
| 5 | tb_purchase | 订单表 |
| 6 | tb_purchase_detail | 订单详情表 |
| 7 | tb_finance_product | 融资产品表 |
| 8 | tb_finance | 融资申请表 |
| 9 | tb_financing_intention | 融资意向表 |
| 10 | tb_expert | 专家信息表 |
| 11 | tb_knowledge | 农业知识表 |
| 12 | tb_discuss | 知识评论表 |
| 13 | tb_question | 问答表 |
| 14 | tb_reserve | 专家预约表 |
| 15 | tb_question_reply | 问题追问回复表 |
| 16 | tb_banner | 轮播图表 |
| 17 | tb_role_application | 角色申请表 |
| 18 | tb_payment | 支付宝支付流水表 |
| 19 | tb_agent_session | agent 会话表 |
| 20 | tb_agent_message | agent 聊天历史表 |
| 21 | tb_agent_tool_log | agent 工具审计表 |
| 22 | tb_system_config | 系统配置表（agent 总开关） |
| 23 | tb_knowledge_chunk | 知识切块+向量表 |

---

### 表结构

#### 1. tb_user（用户表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| user_name | varchar(64) | PK | 用户名 |
| password | varchar(255) | NOT NULL | 密码 |
| real_name | varchar(64) | | 真实姓名 |
| phone | varchar(20) | | 手机号 |
| identity_num | varchar(20) | | 身份证号 |
| address | varchar(255) | | 地址 |
| role | varchar(20) | NOT NULL | farmer/buyer/expert/bank/admin |
| avatar | varchar(255) | | 头像 |
| integral | int | DEFAULT 500 | 积分 |
| credit | int | DEFAULT 5 | 信誉分1-5 |
| status | tinyint | DEFAULT 1 | 1正常 0禁用 |
| create_time | datetime | NOT NULL | |
| update_time | datetime | NOT NULL | |

---

#### 2. tb_address（收货地址表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| id | int | PK AI | |
| own_name | varchar(64) | NOT NULL | 所属用户 |
| consignee | varchar(64) | NOT NULL | 收货人 |
| phone | varchar(20) | NOT NULL | 电话 |
| address_detail | varchar(255) | NOT NULL | 详细地址 |
| is_default | tinyint | DEFAULT 0 | 1默认 0非默认 |

---

#### 3. tb_product（商品表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| order_id | int | PK AI | 商品ID |
| title | varchar(255) | NOT NULL | 标题 |
| price | decimal(10,2) | | 价格 |
| content | varchar(255) | NOT NULL | 内容 |
| picture | varchar(255) | | 图片（API/JSON 字段名 picPath，与知识/轮播图统一；DB 列不变） |
| type | varchar(20) | NOT NULL | goods货源/demand需求 |
| order_status | tinyint | DEFAULT 0 | 0待交易 1交易中 2已完成 |
| own_name | varchar(64) | NOT NULL | 发布者 |
| create_time | datetime | NOT NULL | |
| update_time | datetime | NOT NULL | |

---

#### 4. tb_shoppingcart（购物车表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| shopping_id | int | PK AI | |
| order_id | int | NOT NULL | 商品ID |
| count | int | NOT NULL | 数量 |
| own_name | varchar(64) | NOT NULL | 所属用户 |
| create_time | datetime | NOT NULL | |
| update_time | datetime | NOT NULL | |

---

#### 5. tb_purchase（订单表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| purchase_id | int | PK AI | 订单ID |
| own_name | varchar(64) | NOT NULL | 下单用户 |
| total_price | decimal(10,2) | NOT NULL | 总价 |
| address | varchar(255) | NOT NULL | 收货地址 |
| purchase_status | tinyint | DEFAULT 1 | 1待付款 2待发货 3待收货 4已完成 5已取消 |
| purchase_type | tinyint | NOT NULL | 1购物车 2直接购买 |
| create_time | datetime | NOT NULL | |
| update_time | datetime | NOT NULL | |

---

#### 6. tb_purchase_detail（订单详情表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| detail_id | int | PK AI | |
| purchase_id | int | NOT NULL | 订单ID |
| order_id | int | NOT NULL | 商品ID |
| unin_price | decimal(10,2) | NOT NULL | 单价 |
| count | int | NOT NULL | 数量 |
| sum_price | decimal(10,2) | NOT NULL | 小计 |

---

#### 7. tb_finance_product（融资产品表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| product_id | int | PK AI | 产品ID |
| bank_name | varchar(100) | NOT NULL | 银行名称 |
| introduce | varchar(255) | NOT NULL | 银行介绍 |
| bank_phone | varchar(20) | NOT NULL | 联系电话 |
| money | decimal(12,2) | NOT NULL | 可贷金额 |
| rate | decimal(5,2) | NOT NULL | 年利率% |
| repayment | int | NOT NULL | 还款期限(月) |
| create_time | datetime | NOT NULL | |
| update_time | datetime | NOT NULL | |

---

#### 8. tb_finance（融资申请表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| finance_id | int | PK AI | 申请ID |
| product_id | int | NOT NULL | 产品ID |
| own_name | varchar(64) | NOT NULL | 申请人 |
| real_name | varchar(64) | | 真实姓名 |
| phone | varchar(20) | | 手机号 |
| id_num | varchar(20) | | 身份证号 |
| money | decimal(12,2) | NOT NULL | 申请金额 |
| rate | decimal(5,2) | | 年利率 |
| repayment | int | | 还款期限 |
| status | tinyint | DEFAULT 0 | 0申请中 1已通过 2已驳回 |
| remark | varchar(255) | | 审批备注 |
| combination_name1 | varchar(64) | | 联合人1姓名 |
| combination_phone1 | varchar(20) | | 联合人1电话 |
| combination_idnum1 | varchar(20) | | 联合人1身份证 |
| combination_name2 | varchar(64) | | 联合人2姓名 |
| combination_phone2 | varchar(20) | | 联合人2电话 |
| combination_idnum2 | varchar(20) | | 联合人2身份证 |
| file_info | text | | 附件信息 |
| create_time | datetime | NOT NULL | |
| update_time | datetime | NOT NULL | |

---

#### 9. tb_financing_intention（融资意向表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| id | int | PK AI | |
| user_name | varchar(64) | NOT NULL | 用户 |
| real_name | varchar(64) | NOT NULL | 真实姓名 |
| phone | varchar(20) | NOT NULL | 手机号 |
| address | varchar(255) | NOT NULL | 地址 |
| amount | int | NOT NULL | 期望金额(万) |
| application | varchar(255) | NOT NULL | 融资用途 |
| item | varchar(100) | NOT NULL | 农作物 |
| area | int | | 种植面积(亩) |
| repayment_period | int | NOT NULL | 还款期限(月) |
| create_time | datetime | NOT NULL | |
| update_time | datetime | NOT NULL | |

---

#### 10. tb_expert（专家表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| user_name | varchar(64) | PK | 用户名 |
| real_name | varchar(64) | NOT NULL | 真实姓名 |
| phone | varchar(20) | NOT NULL | 手机号 |
| profession | varchar(100) | NOT NULL | 专业 |
| position | varchar(100) | | 职位 |
| belong | varchar(100) | | 所属单位 |

---

#### 11. tb_knowledge（农业知识表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| knowledge_id | int | PK AI | |
| title | varchar(255) | NOT NULL | 标题 |
| content | varchar(255) | NOT NULL | 内容 |
| pic_path | varchar(255) | | 图片 |
| own_name | varchar(64) | NOT NULL | 发布者 |
| status | tinyint | DEFAULT 1 | 1发布 0草稿 |
| create_time | time | NOT NULL | |
| update_time | datetime | NOT NULL | |

---

#### 12. tb_discuss（知识评论表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| discuss_id | int | PK AI | |
| knowledge_id | int | NOT NULL | 知识ID |
| own_name | varchar(64) | NOT NULL | 评论人 |
| content | varchar(255) | NOT NULL | 内容 |
| create_time | datetime | NOT NULL | |

---

#### 13. tb_question（问答表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| id | int | PK AI | |
| expert_name | varchar(64) | NOT NULL | 专家（存专家 user_name 账号名） |
| expert_real_name | varchar(64) | | 专家真实姓名（冗余，写入时回填） |
| questioner | varchar(64) | NOT NULL | 提问者账号 |
| questioner_real_name | varchar(64) | | 提问者真实姓名（冗余，写入时回填） |
| phone | varchar(20) | NOT NULL | 联系电话 |
| plant_name | varchar(64) | NOT NULL | 农作物 |
| title | varchar(255) | NOT NULL | 问题标题 |
| question | varchar(255) | | 问题内容 |
| answer | varchar(255) | | 最新一条专家回答（列表预览用；完整对话见 tb_question_reply） |
| status | tinyint | DEFAULT 0 | 0未回答 1已回答 2已关闭（结束对话，禁止再回复） |

---

#### 14. tb_reserve（专家预约表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| id | int | PK AI | |
| expert_name | varchar(64) | NOT NULL | 专家（存专家 user_name 账号名） |
| expert_real_name | varchar(64) | | 专家真实姓名（冗余，写入时回填） |
| questioner | varchar(64) | NOT NULL | 预约人账号 |
| questioner_real_name | varchar(64) | | 预约人真实姓名（冗余，写入时回填） |
| phone | varchar(20) | NOT NULL | 电话 |
| address | varchar(64) | NOT NULL | 地址 |
| area | varchar(64) | NOT NULL | 面积 |
| plant_name | varchar(64) | NOT NULL | 农作物 |
| soil_condition | varchar(255) | NOT NULL | 土壤条件 |
| plant_condition | varchar(255) | NOT NULL | 作物条件 |
| plant_detail | varchar(255) | NOT NULL | 作物详情 |
| message | varchar(64) | | 留言 |
| preferred_time | varchar(64) | | 期望时间段（如 2026-08-01 09:00） |
| answer | varchar(64) | | 专家回复 |
| status | tinyint | DEFAULT 0 | 0待处理 1已完成 2已拒绝 |

---

#### 15. tb_question_reply（问题追问回复表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| id | int | PK AI | |
| question_id | int | NOT NULL | 所属问题 tb_question.id |
| author_user_name | varchar(64) | NOT NULL | 回复人账号 |
| author_real_name | varchar(64) | | 回复人真实姓名（冗余） |
| author_role | varchar(16) | NOT NULL | farmer 追问 / expert 回答 |
| content | text | NOT NULL | 回复内容 |
| create_time | datetime | NOT NULL | 回复时间 |

---

#### 16. tb_banner（轮播图表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| banner_id | int | PK AI | |
| title | varchar(100) | | 标题 |
| pic_path | varchar(255) | NOT NULL | 图片 |
| link_url | varchar(255) | | 跳转链接 |
| sort_order | int | DEFAULT 0 | 排序 |
| status | tinyint | DEFAULT 1 | 1启用 0停用 |
| create_time | datetime | NOT NULL | |
| update_time | datetime | NOT NULL | |

#### 17. tb_role_application（角色申请表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| id | int | PK AI | 申请ID |
| user_name | varchar(64) | NOT NULL | 申请人用户名 |
| target_role | varchar(20) | NOT NULL | 申请目标角色 expert/bank |
| real_name | varchar(64) | | 真实姓名 |
| phone | varchar(20) | | 联系电话 |
| profession | varchar(100) | | 专业（申请专家） |
| position | varchar(100) | | 职位（申请专家） |
| belong | varchar(100) | | 所属单位/银行名称 |
| reason | varchar(255) | | 申请理由 |
| materials | varchar(500) | | 相关材料文件URL（资质/证明，可选） |
| status | tinyint | DEFAULT 0 | 0待审核 1通过 2驳回 |
| review_remark | varchar(255) | | 审核备注 |
| reviewer | varchar(64) | | 审核人 |
| create_time | datetime | NOT NULL | |
| update_time | datetime | NOT NULL | |

---

#### 18. tb_payment（支付宝支付流水表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| payment_id | int | PK AI | 支付流水ID |
| purchase_id | int | NOT NULL | 订单ID |
| out_trade_no | varchar(64) | NOT NULL · UNIQUE | 商户订单号（= purchase_id）|
| alipay_trade_no | varchar(64) | | 支付宝交易号 |
| total_amount | decimal(10,2) | NOT NULL | 支付金额 |
| trade_status | varchar(32) | | 交易状态（TRADE_SUCCESS 等）|
| pay_time | datetime | | 支付完成时间 |
| create_time | datetime | NOT NULL | |

---

#### 19. tb_agent_session（agent 会话表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| session_id | varchar(64) | PK | 会话 ID（UUID） |
| user_name | varchar(50) | NOT NULL | 发起人 userName |
| role | varchar(16) | NOT NULL | farmer/buyer |
| status | tinyint | DEFAULT 1 | 1进行中 0已关闭 |
| create_time | datetime | DEFAULT CURRENT_TIMESTAMP | |
| last_active_time | datetime | DEFAULT CURRENT_TIMESTAMP | |

---

#### 20. tb_agent_message（agent 聊天历史表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| id | int | PK AI | |
| session_id | varchar(64) | NOT NULL | 会话 ID |
| user_name | varchar(50) | NOT NULL | 用户名 |
| direction | varchar(16) | NOT NULL | user/assistant |
| content | text | | 消息文本 |
| tool_event | varchar(500) | | 工具事件摘要 JSON（如调用了 query_credit） |
| create_time | datetime | DEFAULT CURRENT_TIMESTAMP | |

---

#### 21. tb_agent_tool_log（agent 工具审计表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| id | int | PK AI | |
| session_id | varchar(64) | NOT NULL | 会话 ID |
| user_name | varchar(50) | NOT NULL | 用户名 |
| tool_name | varchar(64) | NOT NULL | 工具名 |
| arguments | varchar(2000) | | 入参 JSON（脱敏后） |
| result | varchar(2000) | | 结果 JSON（脱敏后） |
| status | varchar(16) | NOT NULL | ok/error/pending/cancelled/timeout |
| duration_ms | int | | 执行耗时（毫秒） |
| confirmed | tinyint | DEFAULT 0 | 写操作是否经用户确认 0否1是 |
| create_time | datetime | DEFAULT CURRENT_TIMESTAMP | |

---

#### 22. tb_system_config（系统配置表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| config_key | varchar(64) | PK | 配置键 |
| config_value | varchar(500) | | 配置值 |
| update_time | datetime | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | |

> 种子数据：`agent_enabled = true`（管理员可通过 `POST /agent/admin/toggle?enabled=false` 切换全局熔断）

---

#### 23. tb_knowledge_chunk（知识切块+向量表）

| 字段 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| id | int | PK AI | |
| knowledge_id | int | NOT NULL | 来源文章 tb_knowledge.knowledge_id |
| chunk_index | int | NOT NULL | 块序号 |
| content | text | NOT NULL | 切块文本（~300 字 + 重叠） |
| embedding | longblob | | bge-m3 向量（1024 维 float 序列化） |
| role_scope | varchar(16) | DEFAULT 'common' | 角色范围 farmer/buyer/common |
| model | varchar(64) | | 向量化模型 |
| create_time | datetime | DEFAULT CURRENT_TIMESTAMP | |

---

## 三、API接口设计

### 基础规范
- 基础路径：`/api`
- 认证：JWT Token（`Authorization: Bearer <token>`）；白名单接口（登录/注册/公开列表/上传/静态资源）不走拦截器
- 返回格式：`{code, message, data}`
- 权限：JWT 拦截器仅验签，角色校验在各控制器内完成（如 `/admin/**` 的 `requireAdmin`）
- 基础设施：MyBatis-Plus 分页拦截器（`MybatisPlusConfig`）、全局异常处理（`GlobalExceptionHandler`，统一 `Result` 返回）、实体主键 `@TableId(type=INPUT)`（如 Expert）

---

### 1. 用户模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| POST | `/user/login` | 登录 | 所有 |
| POST | `/user/register` | 注册 | 所有 |
| GET | `/user/info` | 获取信息 | 已登录 |
| PUT | `/user/info` | 修改信息 | 已登录 |
| PUT | `/user/password` | 修改密码 | 已登录 |

---

### 2. 地址模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| POST | `/address` | 添加 | farmer, buyer |
| GET | `/address/list` | 列表 | farmer, buyer |
| PUT | `/address/{id}` | 修改 | farmer, buyer |
| DELETE | `/address/{id}` | 删除 | farmer, buyer |

---

### 3. 商品模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| POST | `/product` | 发布 | farmer |
| GET | `/product/page` | 分页查询 | 所有 |
| GET | `/product/{orderId}` | 详情 | 所有 |
| PUT | `/product/{orderId}` | 修改 | farmer本人 |
| DELETE | `/product/{orderId}` | 下架 | farmer本人 |
| GET | `/product/user` | 我的发布 | farmer |

> 商品图片字段：API/JSON 为 `picPath`（映射 DB 列 `picture`）。

---

### 4. 购物车模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| POST | `/cart` | 加入 | buyer |
| GET | `/cart/list` | 列表 | buyer |
| PUT | `/cart/{shoppingId}` | 修改数量 | buyer |
| DELETE | `/cart/{shoppingId}` | 删除 | buyer |

---

### 5. 订单模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| POST | `/purchase` | 提交订单 | buyer |
| GET | `/purchase/list` | 我的订单 | buyer |
| GET | `/purchase/{purchaseId}` | 订单详情 | buyer本人 |
| PUT | `/purchase/{purchaseId}/status` | 更新状态 | farmer本人（卖家更新发货状态）|
| DELETE | `/purchase/{purchaseId}` | 取消订单 | buyer本人 |

---

### 6. 融资模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| GET | `/finance/product/list` | 产品列表 | farmer, bank |
| GET | `/finance/product/{productId}` | 产品详情 | farmer, bank |
| POST | `/finance/apply` | 申请融资 | farmer |
| GET | `/finance/apply/list` | 我的申请 | farmer |
| GET | `/finance/apply/bank` | 待审批列表 | bank |
| PUT | `/finance/apply/{financeId}` | 审批 | bank |
| POST | `/finance/intention` | 提交意向 | farmer |
| GET | `/finance/intention/match` | 智能匹配 | bank |

---

### 7. 知识模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| GET | `/knowledge/list` | 知识列表 | 所有 |
| GET | `/knowledge/{knowledgeId}` | 详情 | 所有 |
| POST | `/knowledge` | 发布 | expert, admin |
| PUT | `/knowledge/{knowledgeId}` | 修改 | expert本人, admin |
| DELETE | `/knowledge/{knowledgeId}` | 删除 | expert本人, admin |
| POST | `/knowledge/{knowledgeId}/discuss` | 评论 | farmer, buyer |
| GET | `/knowledge/{knowledgeId}/discuss/list` | 评论列表 | 所有 |
| DELETE | `/knowledge/discuss/{discussId}` | 删除评论 | 本人, admin |

> 公共知识列表仅返回已发布（status=1）；按自身 ownName 查询返回含草稿；新建知识默认 status=1（发布）。

---

### 8. 问答模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| POST | `/question` | 提问（写入 questioner_real_name/expert_real_name） | farmer, buyer |
| GET | `/question/list` | 问答列表 | 所有 |
| GET | `/question/{id}` | 详情 | 已登录（提问人/专家） |
| PUT | `/question/{id}/answer` | 回答（旧接口，兼容；写入回复表，首答置为已回答） | 被指派专家, admin |
| PUT | `/question/{id}/close` | 结束对话（status=2，禁止再回复） | 提问人本人 |
| DELETE | `/question/{id}` | 删除（连带删除追问回复） | 提问人本人, admin |
| GET | `/question/{id}/reply/list` | 追问回复列表（按时间正序） | 所有 |
| POST | `/question/{id}/reply` | 发布追问/回答（多轮对话） | 提问人追问 / 被指派专家回答 |

> **追问多轮对话**：`tb_question_reply` 存全部回复（farmer=追问 / expert=回答）；专家首条回答将 status 0→1 并刷新 `answer`（列表预览用）；status=2（结束对话）后禁止任何回复。显示姓名优先用 `*_real_name`，缺省回退账号。

---

### 9. 专家模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| GET | `/expert/list` | 专家列表 | 所有 |
| GET | `/expert/{userName}` | 专家详情 | 所有 |
| GET/PUT | `/expert/my/info` | 当前专家资料（受保护二级路径，避免与公开 `/expert/{userName}` 在白名单 `/expert/*` 冲突） | expert |
| POST | `/reserve` | 预约 | farmer, buyer |
| GET | `/reserve/my` | 我的预约 | farmer, buyer |
| GET | `/expert/reserve/list` | 专家的预约（按当前专家 token 解析） | expert |
| PUT | `/expert/reserve/{reserveId}/status` | 处理预约（校验处理人为该专家；回填 expert_real_name） | expert |

> **预约字段**：`POST /reserve` 接受 `preferredTime`（期望时间段）并写入 `tb_reserve.preferred_time`；创建时回填 `questioner_real_name`/`expert_real_name`，专家处理时回填 `expert_real_name`。显示姓名优先用 `*_real_name`。

---

### 10. 轮播图模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| GET | `/home` | 首页聚合数据（轮播图+融资+专家+问答+知识+商品） | 所有 |
| GET | `/banner/list` | 轮播图列表 | 所有 |
| POST | `/admin/banner` | 添加 | admin |
| PUT | `/admin/banner/{bannerId}` | 修改 | admin |
| DELETE | `/admin/banner/{bannerId}` | 删除 | admin |

---

### 11. 后台管理模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| GET | `/admin/stats` | 首页统计（商品/融资/知识/问题/预约数量） | admin |
| GET | `/admin/users` | 用户列表（可按 role、keyword 过滤） | admin |
| POST | `/admin/user` | 新增任意角色用户（role=expert 时自动建 tb_expert 资料） | admin |
| PUT | `/admin/user/{userName}` | 修改资料/角色/状态（角色变更同步专家资料） | admin |
| PUT | `/admin/user/{userName}/status` | 启用/禁用 | admin |
| DELETE | `/admin/user/{userName}` | 删除用户（不可删除自己、不可删除最后一个管理员） | admin |
| GET | `/admin/products` | 商品列表（可按 title 模糊） | admin |
| PUT | `/admin/product/{orderId}` | 修改商品（不限归属） | admin |
| DELETE | `/admin/product/{orderId}` | 删除商品 | admin |
| GET | `/admin/finances` | 融资申请列表（可按 status 过滤） | admin |
| GET | `/admin/experts` | 专家列表 | admin |
| POST | `/admin/expert` | 新增专家（同时建 role=expert 用户与 tb_expert） | admin |
| PUT | `/admin/expert/{userName}` | 修改专家资料（同步用户表 realName/phone） | admin |
| DELETE | `/admin/expert/{userName}` | 删除专家（同时删专家资料与用户账号） | admin |

> **融资审批口径**：管理员审核融资复用融资模块的 `PUT /finance/apply/{financeId}`（银行与管理员共用同一审批接口，请求体 `{status, remark}`），未单独实现 `/admin/finance/{financeId}`。
> **实体字段**：商品用 `Product`（`title/content/price/picPath/type/orderStatus/ownName`，无库存字段）；融资用 `Finance`（`realName/money/rate/repayment/status/createTime`，status：0申请中/1已通过/2驳回）。

### 12. 角色申请模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| POST | `/user/apply-role` | 提交角色升级申请（农户/买家→专家/银行；可附带 materials 材料URL） | 已登录（农户/买家） |
| GET | `/user/my/applications` | 我的申请记录 | 已登录 |
| GET | `/admin/applications` | 角色申请列表（可按 status 过滤） | admin |
| PUT | `/admin/application/{id}` | 审核角色申请（通过/驳回；通过则变更用户角色，专家自动建资料） | admin |

---

### 13. 支付模块（支付宝沙箱）

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| POST | `/alipay/pay` | 生成扫码支付二维码（当面付 alipay.trade.precreate），返回二维码链接 qr_code | buyer本人 |
| GET | `/alipay/query` | 主动查询支付结果；成功则订单 1→2 并写入 tb_payment | buyer本人 |
| POST | `/alipay/notify` | 支付宝异步回调通知（RSA2 验签；返回 success/fail） | 公开（支付宝服务器，白名单放行）|

> **公钥模式**：沙箱 AppID / 应用私钥 / 支付宝公钥 三项配在 `application-local.yml`（不入库），网关 gateway 默认值已在 `application.yml`。采用**扫码支付（precreate）**：后端返回二维码链接 `qr_code`，前端用 `qrcode` 库渲染成二维码，买家用「支付宝沙箱版 App」扫码付款；前端每 2.5s 轮询 `/alipay/query` 确认结果。本地开发无需内网穿透（`notify_url` 仅生产环境生效）。商户订单号 `out_trade_no` 直接复用 `purchase_id`，回调据此反查订单并幂等落库。

---

### 14. AI 智能助手模块

| 方法 | URL | 说明 | 权限 |
|:---|:---|:---|:---|
| GET | `/agent/status` | 总开关状态 + 当前角色 | 已登录 |
| POST | `/agent/chat` | 对话（同步 ReAct 循环，带工具调用；写工具挂起时响应含 `form` 表单字段列表） | farmer, buyer |
| POST | `/agent/confirm` | 确认/取消写操作（`{pendingId, accept, sessionId, args?}`；表单卡把用户编辑值放 `args`，后端合并进挂起参数；响应含 `status: executed/cancelled/timeout/rejected/error/disabled`） | pending 属主 |
| GET | `/agent/history/{sessionId}` | 历史消息回看（档2 可见消息；刷新后未决表单卡降级为文本卡） | 会话属主 |
| POST | `/agent/admin/toggle?enabled=` | 切换总开关（全局熔断，upsert 语义） | admin |

> **架构**：手写 ReAct 编排循环（非 Spring AI / LangChain4j）——直接调硅基流动 OpenAI 兼容 `/chat/completions`，解析 `tool_calls` → 查 `ToolRegistry` 分发 → 结果以 `role=tool` 回灌 → 循环（max 6 次）。LLM 模型 = `Qwen/Qwen2.5-72B-Instruct`（主力）/ `Qwen/Qwen2.5-7B-Instruct`（fallback）；Embedding = `BAAI/bge-m3`（1024 维）。
>
> **13 个工具**（8 读 + 5 写）：
> - 读：`query_credit`（信用分/积分）、`query_financing_products`（融资套餐）、`query_match_score`（匹配分/审批通过率）、`search_market`（行情/比价，返回 `商品#编号` 供下单引用，组合词命中失败自动拆词回退）、`list_experts`（可预约专家，返回真实账号/姓名/专业）、`retrieve_knowledge`（RAG 知识检索）、`list_addresses`（地址簿）、`list_my_orders`（我的订单，含状态）
> - 写：`apply_finance`（融资申请，表单卡）、`reserve_expert`（专家预约，表单卡，电话/地址自动取用户资料）、`add_address`（新增收货地址，表单卡，收件人/手机号预填注册资料）、`place_order`（下单，校验商品编号真实存在、自动解析"默认地址/新地址/地址簿#编号"、同人同商品待付款订单附换购提醒）、`cancel_order`（取消待付款订单）
>
> **确认门（HITL）**：写工具不直接落库，先返回操作预览（draft）+ pendingId → 前端展示确认卡 → 用户 `POST /agent/confirm {accept:true/false}` → 才真正调 Service。`PendingActionStore` 内存态，TTL 超时回收（`pendingTtlSeconds`，默认 5min），原子消费（`remove` 一次性取出）防并发双确认。**同一会话任何时刻至多一张有效确认卡**：挂起新 pending 前先作废旧 pending，关闭"先问买苹果、再问买梨、两张卡都点"的顺序双写下单口子（前端同步把旧卡标记失效）。
>
> **表单卡（复杂写工具的槽位收集）**：`reserve_expert` / `apply_finance` / `add_address` 三个工具实现 `Tool.formFields(ctx,args)`——模型信息不全也**直接调用出卡**（preview 不再因缺槽抛异常），已提取的槽位预填进可编辑表单（专家/融资套餐为实时下拉选项、省市区为级联选择、收件人/手机号服务端注入注册资料不经 LLM），缺失槽位由用户在表单里补，**模型无需向用户追问任何字段**。确认时前端把表单值放 `confirm.args`，后端合并（非空覆盖挂起参数、空值删除该键）→ `Tool.validate(merged)` + `preview(merged)` 预检 → **预检失败返回 error 且不消费 pending**，表单保持可编辑、改完可再提交；预检通过才原子消费并 `execute(merged)`。下单/取消仍走纯文字卡。根治了实测中模型反复追问"具体时间/面积/金额/原因"以及把追问措辞当占位值传入参数的问题。
>
> **编排防线（代码层，不依赖模型自觉）**：①假执行拦截——纯文字轮输出"待确认操作/即将下单…确认执行?/下单成功,订单号"等系统专用话术即拦截并纠偏重试；②宣告/槽位追问催促——纯文字轮"我将为您提交"式空宣告、追问用户已给的时间、或表单工具场景下向用户索要槽位（含检索完读工具后停下来用文字索要）时注入催促指令续跑（最多 2 次）；③参数清洗——模型传入的占位串（"周几上午/多少亩/暂无"）、JSON null（hutool 解析为 JSONNull 对象）、字符串 "null" 统一视同未提供；④数字容错——`Args.toInt/toBigDecimal` 剥掉"元/千分位逗号/单位"再解析（模型常传"50000元"）。
>
> **确认端点防越权**：confirm 校验 pending 属主（`ctx.userName` 必须等于当前登录用户，不匹配返回 `rejected` 且不消费）；助手总开关关闭时 confirm 返回 `disabled` 不执行。响应为结构化 `ConfirmOutcome`（status + text），前端按 status 渲染真实结果——只有 `executed` 显示"已执行"，超时/异常/越权各有对应提示，杜绝"失败也显示已执行"。
>
> **会话属主校验**：`getOrCreate` 与 `/agent/history` 均校验 session 归属当前用户（归属不符视同不存在/拒绝），关闭跨用户读写他人会话（IDOR）的口子。
>
> **耗时预算**：编排循环总预算 55s（deadline，每轮 chat 前检查），chat 单次 HTTP 40s、embedding 20s——保证后端在前端 60s 超时前返回，避免"前端报错、后端仍在跑并落库、用户重试产生重复消息"。
>
> **安全**：PII 脱敏（`PiiMasker` 正则匹配手机号/身份证号 → `***` 再喂 LLM，password 永不进 LLM）；系统提示词按角色注入能力边界（农户无下单、买家无融资/预约，越权请求如实拒绝，禁止编造流程与装饰性角色扮演输出）；管理员全局熔断开关（`tb_system_config.agent_enabled`）；工具按角色注册；越权靠 Service 层现有角色校验。
>
> **RAG**：启动时做**增量对账**——已发布文章无 chunk 则切块（~300 字 + 重叠）+ bge-m3 向量化灌入（补新文章）；文章 `update_time` 晚于其最新 chunk 时间则重建（内容已更新）；chunk 指向的文章已删/下架则清孤儿块。之后全量载入内存向量库（不重 embed）。检索按角色 `role_scope` 过滤 + 余弦相似阈值（<0.3 视为无关，返回"暂无相关知识"）；三分支回答（跑题拒答/命中带引用回答/无数据提示）。
>
> **OpenAI 兼容协议**：每条 `role=tool` 消息前必须有一条 assistant 消息携带匹配的 `tool_call_id`（在 `tool_calls` 数组中），否则 API 返回 400。编排循环在模型返回 `tool_calls` 后补回这条桥接消息。

---

## 四、状态值规范

| 类型 | 值 | 说明 |
|:---|:---:|:---|
| 通用开关 | 0/1 | 0关/禁用 1开/正常 |
| 订单状态 | 1-5 | 1待付款 2待发货 3待收货 4已完成 5已取消 |
| 融资状态 | 0-2 | 0申请中 1已通过 2已驳回 |
| 问答状态 | 0/1/2 | 0未回答 1已回答 2已关闭（结束对话，禁止再回复） |
| 预约状态 | 0-2 | 0待处理 1已完成 2已拒绝 |
| 角色申请状态 | 0-2 | 0待审核 1通过 2驳回 |
| agent 会话状态 | 0/1 | 1进行中 0已关闭 |
| agent 工具状态 | 字符串 | ok/error/pending/cancelled/timeout/rejected/disabled |

---

## 五、命名规范

- 表名：`tb_模块_功能`
- 字段名：小写+下划线，如 `user_name`
- 接口路径：`/模块/资源/操作`
- Java类名：大驼峰 `UserService`
- Java方法名：小驼峰 `getUserById`

---

## 六、开发分工

### 按功能角色分配

| 成员 | 负责模块 | 功能角色 | 具体功能 |
|:---|:---|:---|:---|
| **颜达** | 首页展示模块 | 所有用户 | 首页聚合展示、轮播图展示 |
| **颜达** | 农产品交易模块 | 农户 | 发布商品、管理商品、查看订单 |
| **颜达** | 农产品交易模块 | 买家 | 浏览商品、加入购物车、下单、订单管理 |
| **颜达** | 农产品交易模块 | 管理员 | 商品审核与管理（下架/编辑） |
| **何承恩、颜达** | 支付模块 | 买家 | 支付宝沙箱扫码支付（当面付 alipay.trade.precreate）：生成二维码、支付结果轮询查询与落库、订单状态流转（待付款→待发货）、支付流水表 tb_payment |
| **吴金磊** | 个人信息模块 | 所有角色 | 个人中心、编辑资料、地址管理 |
| **吴金磊** | 个人信息模块 | 专家 | 专家个人信息编辑 |
| **吴金磊** | 融资服务模块 | 农户 | 融资申请、我的融资、融资意向 |
| **吴金磊** | 融资服务模块 | 银行 | 融资产品管理、融资审批、智能匹配 |
| **吴金磊** | 融资服务模块 | 管理员 | 融资申请管理 |
| **何承恩** | 专家助力模块 | 农户/买家 | 向专家提问、多轮追问、预约专家（含期望时间）、查看问题/预约记录（显示真实姓名） |
| **何承恩** | 专家助力模块 | 专家 | 回答/追问、管理预约、发布/管理知识文章 |
| **何承恩** | 专家助力模块 | 所有用户 | 浏览专家列表、查看专家详情、浏览知识库 |
| **何承恩** | 后台管理模块 | 管理员 | 用户管理（增删改查用户） |
| **何承恩** | 后台管理模块 | 管理员 | 专家管理（审核/编辑专家信息） |
| **何承恩** | 后台管理模块 | 管理员 | 轮播图管理（添加/编辑/删除） |
| **何承恩** | 公共功能 | 所有用户 | 登录、注册、权限路由守卫 |
| **何承恩** | AI 智能助手模块 | farmer/buyer | 手写 ReAct agent（8 工具 function calling、确认门、PII 脱敏、RAG 检索、审计日志、全局熔断） |
